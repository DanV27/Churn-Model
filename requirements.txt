# Pinned deliberately. churn_model.pkl is a scikit-learn pickle, and newer
# scikit-learn cannot load it — 1.9.0 fails with
# "Can't get attribute '_RemainderColsList'". Re-run model/train.py if you
# bump scikit-learn, so the pickle matches the version that loads it.
scikit-learn==1.6.1
numpy==2.0.2
pandas==2.3.3
fastapi==0.128.8
uvicorn==0.39.0
pydantic==2.13.4
shap==0.49.1
joblib==1.5.3

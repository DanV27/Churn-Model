[pytest]
testpaths = tests
# shap pulls in matplotlib, which emits a wall of pyparsing deprecations on
# import. Not our code and not actionable — keep the CI output readable.
filterwarnings =
    ignore::DeprecationWarning:matplotlib.*
    ignore:.*deprecated.*:UserWarning:matplotlib.*
    ignore::PendingDeprecationWarning
